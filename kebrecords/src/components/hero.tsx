import React from "react";
import './hero.css';

const Hero = () => {
    return (
        <div className="hero">
            <img src='./src/assets/hero.jpg' className="hero-img"/>
        </div>
    );
}

export default Hero;